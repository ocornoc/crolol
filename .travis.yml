language: cpp

os:
  - linux
  - osx

dist: xenial
osx_image: xcode10.1

compiler:
  - gcc
  - clang

matrix:
  exclude:
  - compiler: gcc
    os: osx

addons:
  apt:
    packages:
    - libboost-dev
  homebrew:
    packages:
    - boost

script:
  - make -j debug
